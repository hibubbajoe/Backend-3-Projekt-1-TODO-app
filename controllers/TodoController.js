// var Todo = require("../models/Todo");

// createTodo = (req, res) => {
//   const body = req.body;

//   console.log("Jag är todo controller");

//   //   const todo = new Todo(body);

//   //   todo.save().then(todo => res.json(todo));
// };

// module.exports = {
//   createTodo
// };
